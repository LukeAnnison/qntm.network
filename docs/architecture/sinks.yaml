# DECLARED SINKS (this is declaration, NOT code) — where work LANDS. depth-to-sink is measured
# TO these points, so this file decides what "depth" means for this project.
#
# EMPTY as of 2026-07-23. It declared `rendered-page-written`
# (qntm_network.render.build:write_page) — the Python build's file write. That sink retired with
# the render app when rendering moved into the browser.
#
# The successor is real but undeclarable today: a browser app's terminal effect is the DOM mount
# (app/main.ts:mount), not a disk write. Same reason as flows.yaml — flow-trace captures Python
# only, so a declared TypeScript sink could never be observed, and depth-to-sink would be measured
# to a point nothing reaches. Declared when `typescript-capture-backend` lands.

sinks: []
